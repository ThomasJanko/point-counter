// Unused leftover from an environment probe; kept only because this sandbox
// mount does not support deleting files. Safe to delete manually.
test('unused probe placeholder', () => {
  expect(true).toBe(true);
});
