// This component is no longer used. Changing which players are in a game
// now happens inline on the setup/edit screen (see UserSelection.tsx),
// reached via the game menu's "Modifier la partie" action, matching the
// Tablee reference design, which has no separate "change players" modal.
// Kept as an empty module (rather than deleted) because this sandbox
// cannot delete files; safe to remove manually.
export {};
